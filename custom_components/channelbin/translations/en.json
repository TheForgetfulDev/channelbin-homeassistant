{
  "config": {
    "step": {
      "user": {
        "description": "Before you connect, open ChannelBin and go to Settings > Integrations. Click Generate key and copy the key, since it's only shown once. Then turn the Home Assistant integration switch on. If the switch is off, ChannelBin rejects the key the same way it rejects a wrong one.",
        "data": {
          "host": "Host",
          "port": "Port",
          "scheme": "Scheme",
          "api_key": "API key"
        },
        "data_description": {
          "host": "The address of the machine running ChannelBin, without http:// or a port. For example 192.168.1.50 or channelbin.local.",
          "port": "The port ChannelBin's web page is on. 5000 unless you changed it.",
          "scheme": "Choose https only if ChannelBin sits behind a proxy that serves it over TLS.",
          "api_key": "The key you generated in ChannelBin under Settings > Integrations."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid API key",
      "server_too_old": "ChannelBin at {host} is version {reported}. This integration needs ChannelBin {minimum} or newer. Upgrade ChannelBin, then try again.",
      "server_version_unknown": "ChannelBin at {host} didn't report its version, so it's older than this integration supports. This integration needs ChannelBin {minimum} or newer. Upgrade ChannelBin, then try again."
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "entity": {
    "sensor": {
      "capturing": { "name": "Capturing" },
      "converting": { "name": "Converting" },
      "next_recording": { "name": "Next recording" },
      "disk_free": { "name": "Disk free" },
      "disk_used_percent": { "name": "Disk used" },
      "unread_alerts": { "name": "Unread alerts" },
      "accounts_ok": { "name": "Accounts OK" },
      "accounts_error": { "name": "Accounts with errors" }
    },
    "binary_sensor": {
      "recording": { "name": "Recording" },
      "has_alerts": { "name": "Has alerts" }
    }
  },
  "issues": {
    "server_too_old": {
      "title": "ChannelBin is too old for this integration",
      "description": "ChannelBin at {host} is version {reported}, and this integration needs ChannelBin {minimum} or newer. Its sensors stay unavailable until ChannelBin is upgraded. Once it is, this notice clears by itself."
    },
    "server_version_unknown": {
      "title": "ChannelBin is too old for this integration",
      "description": "ChannelBin at {host} didn't report its version, so it's older than this integration supports. This integration needs ChannelBin {minimum} or newer. Its sensors stay unavailable until ChannelBin is upgraded. Once it is, this notice clears by itself."
    }
  }
}
